export enum Status {
  LOADING = 'loading',
  SUCCESS = 'success',
  ERROR = 'error'
}

export enum MessagesType {
  NOTIFICATION = 'notification',
  MESSAGES = 'messages'
}

export enum DataBasePath {
  JOBS = 'jobs',
  EXPENSES = 'expenses'
}

export enum RoutersPaths {
  MAIN = '/',
  PRIVACY = '/privacy',
  NOFOUND = '*'
}
